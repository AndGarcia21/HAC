function verModelo(tipo) {

    if (tipo === "luminaria") {
        window.location.href = "luminaria.html";
    }

    if (tipo === "caja") {
        window.location.href = "caja.html";
    }
}